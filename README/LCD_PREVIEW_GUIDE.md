# LCD 預覽功能使用指南

## 📺 功能簡介

LCD 預覽功能讓您在拍照時可以看到攝影機的即時畫面，方便調整文件位置和確認拍攝角度。

## ✨ 主要特點

- 🖥️ **即時預覽**: 顯示攝影機即時畫面
- ⏱️ **自動拍照**: 倒數計時後自動拍照
- ⌨️ **手動拍照**: 等待使用者按鍵才拍照
- 🎯 **精確對位**: 確保文件位置正確
- 📊 **視覺回饋**: 顯示拍照倒數和確認訊息

---

## ⚙️ 設定說明

在 `config.ini` 的 `[CAMERA]` 區段中設定：

```ini
[CAMERA]
# 是否顯示攝影機畫面到 LCD 螢幕
show_preview = false

# 預覽視窗名稱
preview_window_name = Book Reader - Camera Preview

# 預覽顯示時間（秒，0 表示持續顯示直到按鍵）
preview_duration = 2.0
```

### 參數說明

| 參數 | 類型 | 預設值 | 說明 |
|------|------|--------|------|
| `show_preview` | 布林值 | `false` | 是否啟用 LCD 預覽 |
| `preview_window_name` | 字串 | `Book Reader - Camera Preview` | 視窗標題 |
| `preview_duration` | 浮點數 | `2.0` | 預覽時間（秒）|

---

## 🎯 三種模式

### 模式 1: 無預覽模式（預設）

**適用場景**: SSH 遠端操作、無螢幕環境、快速辨識

**設定**:
```ini
show_preview = false
```

**運作流程**:
1. 按下按鈕觸發
2. 等待攝影機穩定（0.5秒）
3. 直接拍照
4. 送到 OCR API 辨識

**優點**:
- 速度最快
- 不需要螢幕
- 適合自動化場景

**缺點**:
- 無法確認拍攝角度
- 無法調整文件位置

---

### 模式 2: 自動拍照模式

**適用場景**: 有螢幕、需要預覽、自動化拍攝

**設定**:
```ini
show_preview = true
preview_duration = 2.0  # 或其他秒數（> 0）
```

**運作流程**:
1. 按下按鈕觸發
2. **顯示即時預覽畫面**
3. **螢幕顯示倒數計時**（例如：「拍照倒數: 1.5 秒」）
4. 倒數結束自動拍照
5. 顯示「已拍攝！」確認訊息（1秒）
6. 送到 OCR API 辨識

**優點**:
- 可以看到拍攝畫面
- 有時間調整文件位置
- 自動拍照不需手動操作

**建議時間**:
- 快速確認：`1.0-2.0` 秒
- 充足預覽：`3.0-5.0` 秒

**螢幕顯示範例**:
```
┌────────────────────────────────────┐
│                                    │
│      [即時攝影機畫面]               │
│                                    │
│   拍照倒數: 1.8 秒                  │
│                                    │
└────────────────────────────────────┘
```

---

### 模式 3: 手動拍照模式

**適用場景**: 需要精確對位、複雜文件、品質優先

**設定**:
```ini
show_preview = true
preview_duration = 0  # 設為 0
```

**運作流程**:
1. 按下按鈕觸發
2. **持續顯示即時預覽畫面**
3. **螢幕顯示提示**（「按任意鍵拍照」）
4. 使用者調整文件位置
5. 使用者確認無誤後**按下鍵盤任意鍵**
6. 立即拍照
7. 顯示「已拍攝！」確認訊息（1秒）
8. 送到 OCR API 辨識

**優點**:
- 完全控制拍照時機
- 精確調整文件位置
- 適合複雜或重要文件

**缺點**:
- 需要鍵盤操作
- 處理時間較長

**螢幕顯示範例**:
```
┌────────────────────────────────────┐
│                                    │
│      [即時攝影機畫面]               │
│                                    │
│   按任意鍵拍照                      │
│                                    │
└────────────────────────────────────┘
```

---

## 📋 使用範例

### 範例 1: 書籍閱讀輔助（自動拍照）

**場景**: 視障輔助設備，需要預覽確認但要自動化

**設定**:
```ini
[CAMERA]
camera_device = 0
frame_width = 1920
frame_height = 1080
show_preview = true
preview_window_name = 閱讀輔助器
preview_duration = 3.0
```

**使用步驟**:
1. 將書放在攝影機下方
2. 按下按鈕
3. 看到預覽畫面，確認書頁位置
4. 3 秒倒數後自動拍照
5. 聽取辨識結果

---

### 範例 2: 文件掃描器（手動拍照）

**場景**: 辦公室文件數位化，需要精確對位

**設定**:
```ini
[CAMERA]
camera_device = 0
frame_width = 1920
frame_height = 1080
show_preview = true
preview_window_name = 文件掃描器
preview_duration = 0
```

**使用步驟**:
1. 將文件放在掃描區
2. 按下按鈕
3. 觀察預覽畫面
4. 調整文件位置直到完全對齊
5. 按下空白鍵（或任意鍵）拍照
6. 查看辨識結果

---

### 範例 3: 無頭伺服器（無預覽）

**場景**: SSH 遠端操作，無螢幕環境

**設定**:
```ini
[CAMERA]
camera_device = 0
frame_width = 1280
frame_height = 720
show_preview = false
capture_delay = 0.5
```

**使用步驟**:
1. 將文件放在固定位置
2. 按下按鈕
3. 等待拍照完成
4. 在日誌或終端機查看結果

---

## 🔧 進階技巧

### 技巧 1: 調整預覽視窗大小

預覽視窗會自動調整大小，您可以：
- 使用滑鼠拖曳視窗邊緣調整大小
- 全螢幕顯示（按 F11，視作業系統而定）

### 技巧 2: 多螢幕設置

如果使用多螢幕，預覽視窗會出現在主螢幕上，您可以：
- 拖曳視窗到其他螢幕
- 設定 Raspberry Pi 的主螢幕

```bash
# 設定主螢幕（透過 raspi-config）
sudo raspi-config
# Display Options -> Screen Layout
```

### 技巧 3: 遠端 VNC 使用

如果透過 VNC 遠端連線，預覽功能同樣可用：

```bash
# 在 Raspberry Pi 上啟用 VNC
sudo raspi-config
# Interface Options -> VNC -> Enable

# 然後使用 VNC Viewer 連線
# 預覽視窗會顯示在 VNC 畫面中
```

### 技巧 4: 自動切換模式

根據執行環境自動選擇模式：

```bash
# 建立啟動腳本
cat > start_with_display.sh << 'EOF'
#!/bin/bash
if [ -n "$DISPLAY" ]; then
    # 有顯示環境，啟用預覽
    sed -i 's/show_preview = false/show_preview = true/' config.ini
else
    # 無顯示環境，停用預覽
    sed -i 's/show_preview = true/show_preview = false/' config.ini
fi
python3 book_reader.py
EOF

chmod +x start_with_display.sh
./start_with_display.sh
```

---

## ❓ 常見問題

### Q1: 預覽視窗無法顯示

**可能原因**:
1. 沒有連接螢幕
2. DISPLAY 環境變數未設定
3. 透過 SSH 連線（無 X11 轉發）

**解決方法**:
```bash
# 檢查 DISPLAY 環境變數
echo $DISPLAY

# 如果是空的，設定為預設值
export DISPLAY=:0

# 或改用無預覽模式
nano config.ini
# 設定 show_preview = false
```

### Q2: 預覽畫面延遲或卡頓

**可能原因**:
- 解析度過高
- Raspberry Pi 效能不足
- 攝影機頻寬限制

**解決方法**:
```ini
# 降低解析度
[CAMERA]
frame_width = 640
frame_height = 480
```

### Q3: 按鍵無反應（手動模式）

**可能原因**:
- 預覽視窗未取得焦點
- 鍵盤未正確連接

**解決方法**:
- 用滑鼠點擊預覽視窗確保取得焦點
- 按任意鍵（空白、Enter、ESC 都可以）

### Q4: 想在終端機中使用（無 GUI）

**解決方法**:

使用無預覽模式，或安裝純文字預覽工具：

```bash
# 安裝 fbv（framebuffer viewer）
sudo apt install fbv

# 修改程式使用 framebuffer 而非 X11
# （需要額外開發）
```

---

## 🎛️ 效能比較

| 模式 | 處理時間 | CPU 使用 | 記憶體 | 準確度 |
|------|---------|---------|--------|--------|
| 無預覽 | 最快（5-10秒）| 低 | 低 | 中 |
| 自動拍照 | 中等（7-15秒）| 中 | 中 | 高 |
| 手動拍照 | 較慢（視調整時間）| 中 | 中 | 最高 |

---

## 📝 最佳實踐

1. **測試環境**: 首次使用時建議使用自動拍照模式（2-3秒），確認畫面和位置

2. **生產環境**: 
   - 固定位置拍攝：使用無預覽模式
   - 需要調整：使用自動拍照模式
   - 高品質要求：使用手動拍照模式

3. **效能優化**:
   - SSH 遠端：停用預覽
   - 本地使用：啟用預覽但使用較短的預覽時間
   - 慢速裝置：降低解析度

4. **使用者體驗**:
   - 新手：啟用預覽 + 較長預覽時間（3-5秒）
   - 熟練：啟用預覽 + 較短預覽時間（1-2秒）
   - 自動化：停用預覽

---

## 🔄 與其他功能的整合

### 整合 1: 配合儲存功能

啟用預覽和儲存可以方便檢查：

```ini
[CAMERA]
show_preview = true
preview_duration = 2.0
save_captured_image = true  # 儲存照片
image_save_path = captured_images
```

可以事後檢查 `captured_images/` 目錄中的照片品質。

### 整合 2: 配合日誌記錄

```ini
[LOGGING]
log_level = DEBUG  # 詳細記錄

[CAMERA]
show_preview = true
```

日誌會記錄預覽相關的訊息：
```
2025-11-11 14:30:15 - INFO - 顯示攝影機預覽...
2025-11-11 14:30:17 - INFO - 預覽時間結束，開始拍照
```

---

## 📊 故障排除流程

```
預覽功能有問題？
    ↓
檢查 show_preview 是否為 true
    ↓
檢查是否有連接螢幕
    ↓
檢查 DISPLAY 環境變數
    ↓
嘗試透過 VNC 連線
    ↓
如果都失敗，改用無預覽模式
```

---

## 📞 技術支援

如需更多幫助，請參考：
- [設定檔說明](CONFIGURATION.md)
- [疑難排解指南](TROUBLESHOOTING.md)
- [錯誤訊息說明](ERROR_MESSAGES.md)

---

**文檔版本**: 1.1.0  
**更新日期**: 2025-11-11  
**新增功能**: LCD 預覽功能

